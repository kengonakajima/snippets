package main

import (
	)


type UOmap struct {
}

func UOmapCreate( sz int ) ( m * UOmap ) {
	m = new(UOmap)
	m.
	return
}

// unordered_mapのgolangの実装

/*

 仕様

 -uomapに格納するものはすべて同じサイズの配列。

 
 */
func main() {
	m := UOmapCreate( 1024 )
	
}
